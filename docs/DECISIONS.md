# DECISIONS — Telluride Blues & Brews Festival App

Append-only log of decisions that shape the product, architecture, or process. One entry per decision. Newest at the bottom. Reversing a decision gets a new entry that references the old one; never edit history.

Format: **ID · Date · Status** — Decision. *Context.* *Alternatives.* *Consequences.*

---

**D-001 · 2026-09-09 · Accepted** — **Fresh start.** Discard both prior prototypes (vanilla-JS/Capacitor on `music-app` `main`; Codex `expo-v2`) and build in a new private repository.
*Context:* Sam wants a design-forward, "10x" product and a clean codebase for hand-off to SBG. The prior builds carried divergent architectures and a public repo.
*Alternatives:* continue `expo-v2`; iterate the vanilla build.
*Consequences:* ~1 day of scaffolding; verified content data, font-license research, and App Review notes are reused as inputs, not code.

**D-002 · 2026-09-09 · Accepted** — **Attempt store submission for the 2026 festival (submit by Mon Sep 14), with the GitHub Pages PWA as the guaranteed fallback shipping Sat Sep 12.**
*Context:* 9 days to gates; both developer accounts still pending. Sam chose to try rather than wait for 2027.
*Alternatives:* web-only for 2026; skip 2026.
*Consequences:* native work (icons, push, notifications) is scheduled Day 4; store assets Day 5; buffer Days 7–8. If accounts don't clear, the web app is the 2026 product and nothing is wasted.

**D-003 · 2026-09-09 · Accepted** — **Framework: React 19 + Vite 8 PWA wrapped by Capacitor 8** for iOS/Android.
*Context:* Requirements favor a real web app (design fidelity, GitHub Pages testing, offline PWA) that opens in Xcode/Android Studio as ordinary native projects; the admin console shares the same design system.
*Alternatives:* Expo/React Native (better native feel, weaker web export, EAS/prebuild build path); Flutter (new toolchain).
*Consequences:* native "manners" (tab bar, sheets, haptics, edge-to-edge) must be implemented deliberately in web tech (PLAN §5.1 principle 4) to avoid a web-wrapper feel and Apple 4.2 risk.

**D-004 · 2026-09-09 · Accepted** — **Backend: Firebase** (Auth email/password, Firestore, Cloud Messaging, Functions v2, Hosting, App Check).
*Context:* Need password-protected admin, live content, push without app updates, offline sync, near-zero cost, and a console SBG can own.
*Alternatives:* Supabase + OneSignal (two vendors); Cloudflare Workers/KV + FCM (more custom code).
*Consequences:* Blaze (pay-as-you-go) plan is required for Functions; expected cost ≈ $0–2/month with a $10 budget alert. Firebase Analytics stays disabled.

**D-005 · 2026-09-09 · Accepted** — **Admin access model: 1–3 named SBG staff accounts, email + password, no self-signup, allowlist collection `admins/{uid}`.** No 2FA enforced in v1.
*Alternatives:* single shared login (no audit trail); enforced MFA (friction for non-technical staff under deadline).
*Consequences:* audit log records who published/sent what; MFA can be added in v1.1 via Firebase Auth multi-factor.

**D-006 · 2026-09-09 · Accepted** — **v1 scope = Now, Lineup, Plan (favorites + conflicts + reminders), Alerts (inbox + push), Info, offline-first, admin console.** Brews, map, guide/FAQ, poster-share move to v1.1.
*Context:* Sam selected the two must-have bundles; time box is 9 days.
*Consequences:* poster promises (25+ breweries, tasting sessions) are represented in v1 only by official links from Info and the Now screen; full Brews module is the first v1.1 feature.

**D-007 · 2026-09-09 · Accepted** — **Live content delivery via Firestore `content/published` (single validated doc) read with the JS SDK and persistent cache, plus a build-time bundled snapshot.** Publishing goes through a callable Function that validates, versions, and archives.
*Alternatives:* static JSON regenerated by a GitHub Action (no live updates, slower for SBG); Storage-hosted JSON (no offline sync semantics).
*Consequences:* schedule changes appear in open apps within seconds; the app never trusts unvalidated remote data; history enables rollback.

**D-008 · 2026-09-09 · Accepted** — **Push architecture: one FCM topic (`festival-2026`); alert doc creation triggers a Function that sends the push; no per-device tokens stored by us.** Alerts inbox in Firestore is the primary channel; push is enhancement.
*Consequences:* iOS web push only works for Home-Screen-installed PWAs; native apps get full push once the APNs key exists (needs the Apple account). Rate limits (6/hour, 30/day) enforced server-side.

**D-009 · 2026-09-09 · Accepted** — **Hosting split: fan web app on GitHub Pages (private repo, GitHub Pro); admin console on Firebase Hosting.**
*Context:* Sam wants GitHub Pages for testing; admin benefits from Firebase Hosting's auth-domain integration and headers.
*Consequences:* two deploy workflows; Pages project-subpath requires `base` config in Vite and relative service-worker scope.

**D-010 · 2026-09-09 · Accepted** — **Typography: Bungee (display), Bungee Shade (numerals/day headers), Michroma (wide labels), DM Sans (UI/body); all OFL, bundled locally.** Poster fonts (Beastly, Eurostile Extended, Futura PT) are never bundled; official lockups are used as images.
*Alternatives:* previous Archivo Black + DM Sans (safer, less character); licensing Adobe Fonts for app embedding (cost/time).
*Consequences:* record licenses in `docs/FONT_LICENSES.md`; verify Bungee readability at small sizes — use only ≥ 24 px.

**D-011 · 2026-09-09 · Accepted** — **Artwork approach: Sam exports true poster layers from the PSD where feasible; ChatGPT generates only supporting elements in the poster's illustration language; no generated musicians, no re-drawn logo, no re-typeset wordmark.** Details in `ASSET-BRIEF.md`.
*Consequences:* hero parallax and per-screen backdrops depend on layer delivery; flattened-poster crops are the fallback with identical file names so code doesn't change.

**D-012 · 2026-09-09 · Accepted** — **Privacy posture: no fan accounts, no analytics, no ads, no location; favorites/plan on-device only.** Expected Apple label "Data Not Collected" and Play Data safety "no data collected" — to be verified against the final binary and current store guidance before answering.

**D-013 · 2026-09-09 · Accepted** — **Palette and tokens sampled from the poster (PLAN §5.2) are provisional until checked against PSD swatches on Day 0; then locked.** Dark mode = "poster at night" (`night`/`night-deep` surfaces, paper text).

**D-014 · 2026-09-09 · Accepted** — **Documentation set: `PLAN.md`, `HANDOFF.md`, `DECISIONS.md`, `ASSET-BRIEF.md`, `CLAUDE.md`, kept in `docs/` of the repo and mirrored to the Claude Project "Blues and Brews Fable 5.1".** HANDOFF is updated at every phase boundary; DECISIONS is append-only.

**D-015 · 2026-09-09 · Accepted** — **Store publishing specifics: iPhone-only for v1; honest age-rating answers (expect 9+/13+ on Apple, Teen-range on Play) rather than 4+; app records under SBG's developer accounts where possible, otherwise a signed SBG authorization letter for Apple 5.2.1; Android targetSdk 36; manual release on both stores.** Full field-by-field procedure lives in `STORE-CHECKLIST.md`.
*Context:* review of the plan found the earlier "4+/Everyone" assumption wrong for an app with brewery and tasting content, iPad support would force iPad screenshots, and publishing SBG's brand from a personal account is a known rejection trigger.
*Consequences:* store copy avoids over-promising v1.1 features; iPad layout deferred to v1.1; ownership question (PLAN §12 Q1) becomes the first thing to resolve.

**D-016 · 2026-09-09 · Accepted** — **Palette locked from poster pixel samples; stage colors assigned.** Supersedes the "provisional" status in D-013. Key values: paper `#EBD5B3`, ink `#1E1A1A`, sky `#1890A8`, night `#1A4A80` / night-deep `#0A3070`, sun `#F0C41C`, sun-hot `#F09A1C`, ember `#D4452F`, pine `#1E7A22`, leaf `#3CA81E`, plum `#78307A`, violet `#7A64A8`. Stages: Main = sky, Blues = plum, Truck = pine, Campground Sessions = violet (`Stage.color` is a token name in the schema).
*Context:* region-quantized samples of `poster-preview-1080x1890.png` showed the poster is lighter and more saturated than the D-013 guesses. Reviewed by Sam in the design-system preview.
*Consequences:* tokens live in `packages/shared` and are the single source for app and (later) admin; D-010's "Bungee ≥ 24 px" is relaxed to ≥ 17 px for headliner list rows only.

**D-017 · 2026-09-09 · Accepted** — **First implementation pass is a local design pass: real code, all five tabs at rough fidelity, no backend.** Repo shape: monorepo with only `apps/festival`, `packages/shared`, `packages/content`; `apps/admin`, `functions`, `firebase/` are created on their plan days. Hero art uses flattened-poster crops under the ASSET-BRIEF file names; all other ornaments are code-drawn SVG/CSS. Spec: `docs/superpowers/specs/2026-09-09-festival-app-design-pass-design.md`.
*Alternatives:* mockups-then-code; Now + Lineup only; flat single app refactored later.
*Consequences:* design decisions land as shippable components; Firestore/Capacitor/PWA attach behind existing interfaces (`ContentRepository`, `platform/`) without screen changes.

**D-018 · 2026-09-09 · Accepted** — **Two public GitHub repositories with generic names: `samgumble/festival-app` (fan app monorepo: `apps/festival`, `packages/shared`, `packages/content`) and `samgumble/festival-admin` (SBG console).** Supersedes the single-private-repo assumption in D-001/D-009 and removes `apps/admin` from the monorepo layout in PLAN §3.2.
*Context:* Sam wants the app and the admin console in separate repos, public for now, with names that don't advertise the festival until launch.
*Alternatives:* one private monorepo (original plan); private repos.
*Consequences:* the admin console must consume `@bb/shared` (schema, tokens) from another repo — decide on Day 3 between publishing `@bb/shared` to GitHub Packages or vendoring via git subtree. Licensed artwork derivatives (poster preview, lockups) are publicly visible; the PSD remains git-ignored and local. `docs/HANDOFF.md §2` records both URLs.

**D-019 · 2026-09-09 · Accepted** — **Contrast adjustments after the design-pass review.** Section eyebrows use `night` (6.3:1 on paper) instead of `sky` (2.6:1) for text in light mode; `sky` remains the structure *fill* color (`--structure-fill`). Sky chips carry `ink` text (4.6:1) instead of white (3.8:1). Ember chips keep white text at 4.48:1 — a known 0.02 shortfall against AA 4.5, accepted for v1 because darkening the locked ember hex would drift from the poster; revisit with SBG. *Consequences:* PLAN §5.2 "sun never carries white text" now extends to sky; the mockups' sky eyebrows are superseded.

**D-020 · 2026-09-09 · Accepted** — **`@bb/shared` reaches the admin repo via git subtree** (`festival-app/packages/shared` → `festival-admin/shared`), synced with `git subtree pull`.
*Alternatives:* GitHub Packages (needs a token on every install, even for public packages); npm public registry (publishing overhead for a two-consumer package).
*Consequences:* schema/token changes land in `festival-app` first, then one subtree pull in the admin repo; the admin repo never edits `shared/` directly.

**D-021 · 2026-09-09 · Accepted** — **Firebase on the Spark (free) plan; no Cloud Functions, so no push notifications in this phase; publishing runs in the admin console's browser under Firestore rules.** Project `bb-festival-2026`, owner sam.gumble@gmail.com, Firestore `(default)` in `nam5`. Supersedes the Blaze assumption in D-004 and the `publishContent` callable in D-007 for 2026; both return unchanged if Blaze is enabled later.
*Context:* Sam wants zero cost and no card for now; the Alerts inbox is live via Firestore, which D-008 already made the primary channel.
*Consequences:* the admin console validates with the shared Zod schema and writes `published` + `history` in one atomic batch; rate limits on alerts are UI-side only; iOS/Android push is deferred to the native phase and needs Blaze.

## D-022 — PWA via vite-plugin-pwa (generateSW, prompt updates); WebP art committed; interim code-drawn icon (2026-09-10)

**Decision.** The web app becomes a PWA with `vite-plugin-pwa` 1.3.0 in `generateSW` mode, `registerType: "prompt"` (update banner, never a silent reload), `navigateFallback` to `index.html`, no runtime caching (Firestore persistence owns data). Poster art ships as WebP generated by `npm run art:build` (Homebrew `cwebp`; opaque layers lossy q82, alpha files and the official lockup lossless) with the outputs committed and the PNGs kept as sources. The app icon is a code-drawn sun on the sky→night gradient per ASSET-BRIEF §2, rasterized by Playwright, until the PSD-derived icon is exported. Install nudge lives on Info only. Spec: `docs/superpowers/specs/2026-09-10-pwa-and-art-budget-design.md`.

**Why.** generateSW is the least code for precache + versioning + cleanup, the three places hand-rolled workers break offline; `sharp`/asset generators aren't on the approved dependency list, while `cwebp` and Playwright already exist on the machine; committing generated assets keeps CI free of native image tooling. The lockup stays lossless because logo lockups must ship unmodified.

**Consequences.** Art 3.2 MB → ~0.95 MB and fonts 1.0 MB → 0.35 MB as WOFF2 (`npm run fonts:build`, Homebrew `woff2`); precache ≈ 2.55 MB (budget 3 MB). Icon files are a drop-in replacement later. Offline e2e runs locally (`npm run e2e:offline`), not in the Pages workflow.

## D-023 — Capacitor 8 native shell; reminders as one switch over all favorites via local notifications (2026-09-10)

**Decision.** The fan app is wrapped with Capacitor 8.5.1 (`apps/festival/ios`, `apps/festival/android`, committed without signing material), bundle ID `com.sbgproductions.bluesandbrews`, iPhone-only portrait v1, Android target SDK 36. Set reminders are a single "Remind me before my sets" switch (native only) that schedules a local notification `leadMinutes` before every favorited upcoming set and reconciles by diff when favorites, lead time or published times change; no per-set toggles. Platform APIs sit behind `src/platform/*` adapters with web no-op fallbacks; the service worker, install nudge and update banner stay web-only. Interim icon/splash reuse the code-drawn sun and the official lockup. Spec: `docs/superpowers/specs/2026-09-10-native-shell-and-reminders-design.md`.

**Why.** One codebase, three targets, as PLAN §3 chose; Sam rejected per-set toggles on web, and a single switch keeps the Plan screen clean while still delivering the "phone in pocket" reminder. Apple enrollment is pending, so the phase targets simulator + side-load and leaves TestFlight as a checklist item. Push remains Blaze-gated (D-021).

**Consequences.** `reminders: string[]`/`toggleReminder` are removed from the plan store (persist migration). No `@capacitor/preferences` or `Filesystem`: localStorage persistence and text-only sharing of the `.ics` in v1. Native builds are not part of CI.

**Consequences (addendum, 2026-09-10).** iOS project uses Swift Package Manager (Capacitor 8 template default); legacy Android round icon rendered with transparent corners (review fix).

**Consequences (addendum, 2026-09-10).** Native origins stay at the Capacitor defaults (`capacitor://localhost` iOS, `https://localhost` Android): fans' favorites/plan live in WebView localStorage at that origin, so `ios.scheme`/`androidScheme` must never change. Android uses the Android 12 icon-only system splash on a paper background (no bitmap splash drawables), a deliberate deviation from spec §8.
