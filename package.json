{
  "name": "telluride-blues-brews-companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/server.mjs",
    "preview": "node scripts/server.mjs dist 4173",
    "native:sync": "npm run build && cap sync --bundle-id org.tellurideblues.companion --web-dir dist",
    "native:ios": "npm run build && cap sync ios && cap open ios",
    "native:android": "npm run build && cap sync android && cap open android"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/cli": "8.4.3",
    "@capacitor/core": "^8.5.1",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/ios": "^8.5.1",
    "@capacitor/local-notifications": "^8.3.1",
    "@capacitor/share": "^8.0.1"
  }
}
